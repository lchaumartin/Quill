// Quill for Unity — a declarative, reactive UI framework.
// Copyright (c) 2026 Leo CHAUMARTIN. Licensed under the MIT License - see LICENSE.md.
//
// ProgressBar — Tome: an inked frame filling with wax red.
// Same API in every theme: value, from, to, position (read-only)
Rectangle {
    id: control

    property real value: 0
    property real from: 0
    property real to: 1
    property real position: control.to > control.from ? Math.clamp((control.value - control.from) / (control.to - control.from), 0, 1) : 0

    width: 240
    height: 12
    radius: 2
    color: Theme.control
    border.width: Theme.borderWidth
    border.color: Theme.border

    Rectangle {
        x: 3; y: 3
        width: (control.width - 6) * control.position
        height: control.height - 6
        color: Theme.accent
        visible: width > 0.5
    }
}
