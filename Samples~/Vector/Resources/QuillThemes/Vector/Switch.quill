// Quill for Unity — a declarative, reactive UI framework.
// Copyright (c) 2026 Leo CHAUMARTIN. Licensed under the MIT License - see LICENSE.md.
//
// Switch — Vector: a hairline slot with ON / OFF readouts; the block jumps to the lit side.
// Same API in every theme: text, checked, enabled, signal toggled
Item {
    id: control

    property string text: ""
    property bool checked: false
    property bool enabled: true
    signal toggled

    width: track.width + (control.text.length > 0 ? 12 + label.contentWidth : 0)
    height: Math.max(track.height, label.contentHeight)
    opacity: control.enabled ? 1 : 0.35

    property real position: control.checked ? 1 : 0
    Behavior on position { NumberAnimation { duration: Theme.duration; easing.type: Easing.OutQuad } }

    Rectangle {
        id: track
        width: 58
        height: 22
        anchors.verticalCenter: parent.verticalCenter
        color: area.containsMouse ? Theme.controlHover : Theme.control
        border.width: Theme.borderWidth
        border.color: control.checked ? Theme.accent : Theme.border

        Rectangle {
            x: 3 + control.position * (track.width - width - 6)
            y: 3
            width: 26
            height: track.height - 6
            color: control.checked ? Theme.accent : Theme.border
        }
        Text {
            text: control.checked ? "ON" : "OFF"
            color: control.checked ? Theme.accent : Theme.textMuted
            fontSize: 10
            font.family: Theme.font
            font.letterSpacing: 1
            x: control.checked ? 7 : track.width - contentWidth - 6
            anchors.verticalCenter: parent.verticalCenter
        }
    }

    Text {
        id: label
        text: control.text
        color: control.checked ? Theme.text : Theme.textMuted
        fontSize: Theme.fontSize
        font.family: Theme.font
        anchors.left: track.right
        anchors.leftMargin: 12
        anchors.verticalCenter: parent.verticalCenter
    }

    MouseArea {
        id: area
        anchors.fill: parent
        hoverEnabled: true
        enabled: control.enabled
        onClicked: { control.checked = !control.checked; control.toggled() }
    }
}
