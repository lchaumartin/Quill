// Quill for Unity — a declarative, reactive UI framework.
// Copyright (c) 2026 Leo CHAUMARTIN. Licensed under the MIT License - see LICENSE.md.
//
// CheckBox — Pebble: a soft rounded tile; checking fills it with coral and pops a dot in.
// Same API in every theme: text, checked, enabled, signal toggled
Item {
    id: control

    property string text: ""
    property bool checked: false
    property bool enabled: true
    signal toggled

    width: box.width + (control.text.length > 0 ? 12 + label.contentWidth : 0)
    height: Math.max(box.height, label.contentHeight)
    opacity: control.enabled ? 1 : 0.45

    property real mark: control.checked ? 1 : 0
    Behavior on mark { SpringAnimation { spring: 6; damping: 0.3 } }

    Rectangle {
        id: box
        width: 26
        height: 26
        radius: 9
        anchors.verticalCenter: parent.verticalCenter
        color: Qt.tint(area.containsMouse ? Theme.controlHover : Theme.control, Qt.alpha(Theme.accent, Math.clamp(control.mark, 0, 1)))

        Rectangle {
            width: 10 * control.mark
            height: width
            radius: width / 2
            anchors.centerIn: parent
            color: Theme.accentText
            visible: width > 0.5
        }
    }

    Text {
        id: label
        text: control.text
        color: Theme.text
        fontSize: Theme.fontSize
        font.family: Theme.font
        anchors.left: box.right
        anchors.leftMargin: 12
        anchors.verticalCenter: parent.verticalCenter
    }

    MouseArea {
        id: area
        anchors.fill: parent
        hoverEnabled: true
        enabled: control.enabled
        onClicked: { control.checked = !control.checked; control.toggled() }
    }
}
