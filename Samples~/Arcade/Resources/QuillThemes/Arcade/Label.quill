// Quill for Unity — a declarative, reactive UI framework.
// Copyright (c) 2026 Leo CHAUMARTIN. Licensed under the MIT License - see LICENSE.md.
//
// Label — Arcade: Orbitron capitals for titles and headings (headings in the accent), Rajdhani body.
// Same API in every theme: text, kind: "title" | "heading" | "body" (default) | "caption"
Text {
    id: label

    property string kind: "body"

    color: label.kind == "heading" ? Theme.accent : label.kind == "caption" ? Theme.textMuted : Theme.text
    font.family: label.kind == "title" || label.kind == "heading" ? Theme.titleFont : Theme.font
    font.capitalization: label.kind == "title" || label.kind == "heading" ? Font.AllUppercase : Font.MixedCase
    font.letterSpacing: label.kind == "title" ? 4 : label.kind == "heading" ? 2 : 0
    fontSize: label.kind == "title" ? Theme.titleSize
            : label.kind == "heading" ? 15
            : label.kind == "caption" ? 16
            : Theme.fontSize
    lineHeight: 1.15
}
